/* ================================================================
   KALGOTH'S GAZE – CLEAN ALTAR EDITION
   Smooth fade‑in assembly, refined typography, prominent images,
   and removal of dead UI elements.
   ================================================================ */
@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Inter:wght@400;500&display=swap');

:root {
  --void-deep: #05020a;
  --void-mid: #0f0814;
  --stone-base: rgba(20, 14, 10, 0.95);
  --stone-dark: rgba(12, 8, 5, 0.98);
  --gold-base: #b89248;
  --gold-light: #f0d580;
  --gold-highlight: #fff8e0;
  --gold-shadow: #7a6336;
  --crimson-base: #6a1b1b;
  --crimson-glow: #ff1a1a;
  --crimson-deep: #3a0505;
  --arcane-purple: #4a1a6a;
  --arcane-glow: #d4af37;
  --arcane-deep: #1a052a;
  --nature-green: #2d4a1e;
  --nature-glow: #7ea04b;
  --nature-deep: #121e0f;
  --panel-border-radius: 32px;
  --btn-border-radius: 28px;
  --anim-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --anim-smooth: cubic-bezier(0.4, 0, 0.2, 1);
}

*,
*::before,
*::after {
  box-sizing: border-box;
  user-select: none;
  margin: 0;
  padding: 0;
}

img {
  max-width: 100%;
  height: auto;
  object-fit: contain;
  display: block;
  image-rendering: crisp-edges;
}

html,
body {
  margin: 0;
  padding: 0;
  height: 100%;
  overflow: hidden;
  font-family: 'Inter', 'Segoe UI', sans-serif;
  font-size: 15px;
  color: #e8dcb0;
  -webkit-font-smoothing: antialiased;
  background-color: var(--void-deep);
}

/* Background art with texture overlay */
body {
  background-image:
    url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E"),
    url('/Images/Game%20Art/UI%20Elements/Main%20Game%20Background%20Image%20V2.png');
  background-size: auto, cover;
  background-position: center;
  background-blend-mode: normal;
  box-shadow: none;
}

/* Vignette overlay (violet) */
body::after {
  content: '';
  position: fixed;
  inset: 0;
  background: radial-gradient(circle at center, rgba(40,10,40,0.3) 0%, rgba(0,0,0,0.9) 80%);
  pointer-events: none;
  z-index: -1;
  mix-blend-mode: multiply;
}

/* ── GAME CONTAINER ── */
#gameContainer {
  position: fixed;
  inset: 0;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  overflow-x: hidden;
  background: transparent;
  opacity: 0;
  transition: opacity 1s var(--anim-smooth);
  z-index: 1;
  pointer-events: none;
}

#gameContainer.altar-active {
  opacity: 1;
  pointer-events: auto;
}

/* ── ICON BAR ── */
.icon-bar {
  display: flex;
  gap: 16px;
  justify-content: flex-end;
  padding: 10px 16px;
  z-index: 300;
  flex-shrink: 0;
  align-items: center;
}

.game-icon {
  width: 64px; height: 64px;
  background: var(--stone-dark);
  backdrop-filter: blur(20px) saturate(180%);
  border-radius: 24px;
  border: 1px solid rgba(184, 146, 72, 0.3);
  box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  cursor: pointer;
  transition: transform 0.3s var(--anim-spring), box-shadow 0.3s;
  position: relative;
  overflow: hidden;
  display: flex; align-items: center; justify-content: center;
}

.game-icon::before {
  content: '';
  position: absolute;
  inset: -50%;
  background: conic-gradient(from 0deg, transparent 0%, rgba(255,255,255,0.1) 25%, transparent 50%, rgba(255,255,255,0.1) 75%, transparent 100%);
  animation: iconSheen 4s linear infinite;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s;
}

.game-icon:hover::before { opacity: 1; }
.game-icon:hover {
  transform: translateY(-8px) scale(1.05);
  border-color: var(--gold-light);
  box-shadow: 0 0 30px rgba(240, 213, 128, 0.25), 0 20px 40px rgba(0,0,0,0.6);
}

.game-icon img {
  width: 60%; height: 60%;
  object-fit: contain;
  z-index: 1;
  filter: drop-shadow(0 4px 6px rgba(0,0,0,0.8));
}

@keyframes iconSheen { 100% { transform: rotate(360deg); } }

/* ── MAIN GRID ── */
.main-grid {
  flex: 1;
  display: grid;
  grid-template-columns: 300px 1fr 340px;
  gap: 28px;
  padding: 24px 32px 40px;
  overflow: visible !important;
  position: relative;
  z-index: 2;
  align-items: start;
}

/* ── PANELS (STONE TABLETS) – smooth fade‑in assembly ── */
.panel {
  background: linear-gradient(160deg, var(--stone-base) 0%, var(--stone-dark) 100%);
  border-radius: var(--panel-border-radius);
  padding: 24px;
  position: relative;
  box-shadow:
    0 25px 60px rgba(0,0,0,0.85),
    0 0 0 1px rgba(184, 146, 72, 0.15),
    0 0 80px rgba(0,0,0,0.5);
  transition: transform 0.2s, box-shadow 0.3s;
  display: flex; flex-direction: column;
  gap: 20px;
  height: fit-content;
}

/* Golden border sheen */
.panel::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: var(--panel-border-radius);
  padding: 2px;
  background: linear-gradient(135deg, var(--gold-base) 0%, var(--gold-light) 25%, var(--gold-base) 50%, var(--gold-shadow) 75%, var(--gold-base) 100%);
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  pointer-events: none;
  z-index: 1;
  animation: borderSheen 8s linear infinite;
}

@keyframes borderSheen {
  0% { filter: brightness(1) hue-rotate(0deg); }
  50% { filter: brightness(1.2) hue-rotate(10deg); }
  100% { filter: brightness(1) hue-rotate(0deg); }
}

/* Inner depth shadow */
.panel::after {
  content: '';
  position: absolute;
  inset: 24px;
  border-radius: calc(var(--panel-border-radius) - 10px);
  box-shadow: inset 0 0 60px rgba(0,0,0,0.6);
  pointer-events: none;
  z-index: -1;
}

/* ── SMOOTH FADE IN (replaces tablet slam) ── */
.left-panel,
.center-stack,
.right-panel {
  opacity: 0;
  animation: fadeInPanel 0.8s var(--anim-smooth) forwards;
}

.left-panel  { animation-delay: 0.1s; }
.center-stack { animation-delay: 0.25s; }
.right-panel  { animation-delay: 0.4s; }

@keyframes fadeInPanel {
  to { opacity: 1; }
}

/* ── TYPOGRAPHY ── */
h1, h2, h3, h4, h5, h6,
.player-name, .console-header, .craft-btn,
.invoke-btn, .tome-tab {
  font-family: 'Cinzel', serif;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-shadow: 0 0 8px rgba(200,180,100,0.5);
}

/* ── BUTTONS ── */
.craft-btn, .invoke-btn {
  background: linear-gradient(145deg, #2a1a12, #0f0804);
  border: 2px solid var(--gold-base);
  color: #f5efe0;
  padding: 8px 12px;
  border-radius: var(--btn-border-radius);
  cursor: pointer;
  font-size: 0.8rem;
  font-weight: 500;
  box-shadow: 0 4px 0 #050000, 0 4px 8px rgba(0,0,0,0.5),
    inset 0 1px 3px rgba(255,255,255,0.08), inset 0 -1px 2px rgba(0,0,0,0.5),
    0 0 10px rgba(201,164,92,0.2);
  transition: all 0.2s ease;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  font-family: 'Cinzel', serif;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  position: relative;
  overflow: hidden;
}

.craft-btn::before, .invoke-btn::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255,255,200,0.2) 0%, transparent 50%);
  border-radius: 26px;
  opacity: 0;
  animation: buttonShine 2s ease-in-out infinite;
  pointer-events: none;
}

@keyframes buttonShine {
  0%, 100% { opacity: 0; }
  50% { opacity: 0.4; }
}

.craft-btn:hover, .invoke-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 7px 0 #050000, 0 8px 14px rgba(0,0,0,0.6),
    inset 0 1px 4px rgba(255,255,255,0.15),
    0 0 24px rgba(212,175,55,0.4);
  border-color: var(--gold-light);
  color: #ffd700;
}

.craft-btn:active, .invoke-btn:active {
  transform: translateY(1px);
  box-shadow: 0 2px 0 #050000, 0 2px 4px rgba(0,0,0,0.4),
    inset 0 1px 3px rgba(255,255,255,0.08);
}

.invoke-btn {
  width: 100%;
  padding: 16px;
  font-size: 1.3rem;
  font-weight: 700;
  letter-spacing: 6px;
  background: linear-gradient(145deg, #5a1a1a, #2a0505);
  border-color: var(--crimson-glow);
  box-shadow: 0 0 30px #6a2a2a, 0 5px 0 #050000, inset 0 1px 3px rgba(255,255,255,0.1);
}

/* ── PROGRESS BARS ── */
.progress-bar {
  background: linear-gradient(90deg, #0a0205, #1a0a0a);
  height: 10px;
  border-radius: 12px;
  overflow: hidden;
  border: 1px solid var(--gold-base);
  box-shadow: inset 0 1px 2px rgba(0,0,0,0.6), 0 2px 4px rgba(0,0,0,0.3);
  margin: 6px 0;
  position: relative;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #c8a070, #e8daa0);
  box-shadow: 0 0 12px var(--gold-base), inset 0 1px 2px rgba(255,255,255,0.4);
  transition: width 0.4s cubic-bezier(0.4, 0.0, 0.2, 1);
}

.progress-fill.low {
  background: linear-gradient(90deg, #c00, #f44);
  box-shadow: 0 0 12px var(--crimson-glow);
}

/* ── BATTLE CANVAS LAYER ── */
.battle-canvas-layer {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 5000;
}

/* ── PERSPECTIVE CARDS (still used in battle modal) ── */
.card-perspective {
  perspective: 600px;
  width: 200px;
  aspect-ratio: 3/4;
  margin: 0 auto;
}
.card-perspective .battle-card {
  width: 100%;
  height: 100%;
  border-radius: 12px;
  overflow: hidden;
  background: #0a0508;
  transform: rotateY(0deg);
  transition: transform 0.3s ease;
}
.player-card .battle-card { transform: rotateY(5deg); }
.enemy-card .battle-card { transform: rotateY(-5deg); }

/* ── CRAFT TILES ── */
.crafting-tiles { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }
.tile {
  width: 90px; height: 90px;
  background: linear-gradient(145deg, #2a1a12, #0f0804);
  border: 2px solid var(--gold-base);
  border-radius: 24px;
  box-shadow: 0 6px 0 #050000, 0 8px 16px rgba(0,0,0,0.6),
    inset 0 1px 4px rgba(255,255,255,0.1), inset 0 -1px 4px rgba(0,0,0,0.5),
    0 0 12px rgba(201,164,92,0.15);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  cursor: pointer; transition: all 0.2s ease; padding: 6px;
}
.tile:hover {
  transform: translateY(-4px);
  border-color: var(--gold-light);
  box-shadow: 0 8px 0 #050000, 0 12px 20px rgba(0,0,0,0.7),
    inset 0 1px 4px rgba(255,255,255,0.15),
    0 0 20px rgba(212,175,55,0.3);
}
.tile img { width: 48px; height: 48px; filter: drop-shadow(0 3px 3px rgba(0,0,0,0.5)); }
.tile-label { font-size: 0.7rem; font-weight: 600; color: #f0e8d8; margin-top: 4px; }

/* ── MAZE ENTRANCE CARD (enlarged icon) ── */
.maze-entrance-card {
  display: flex; align-items: center; gap: 18px;
  background: rgba(12,6,18,0.2); border: 1px solid var(--gold-base); border-radius: 24px;
  padding: 18px; margin-bottom: 0;
  box-shadow: 0 4px 0 #0a0508, inset 0 1px 2px rgba(255,255,255,0.05);
}
.maze-avatar {
  width: 120px; height: 120px;               /* was 70px – now larger */
  border-radius: 20px;
  border: 2px solid var(--gold-light);
  object-fit: cover;
}

/* ── FAMILIAR CARD ── */
.familiar-card {
  background: linear-gradient(135deg, rgba(126,160,75,0.15), rgba(90,120,60,0.08));
  border-radius: 24px; padding: 16px; margin-top: 10px;
  border: 2px solid var(--nature-glow);
  box-shadow: 0 0 20px rgba(90,120,70,0.25), inset 0 1px 4px rgba(255,255,255,0.1),
    inset 0 -1px 4px rgba(0,0,0,0.3), 0 8px 16px rgba(0,0,0,0.4);
}
.whisp-sprite-large {
  width: 56px; height: 56px;
  background: radial-gradient(circle at 35% 35%, #b0d080, #5a7a3a 60%, #2a4a1a);
  border-radius: 50%;
  box-shadow: 0 0 16px var(--nature-glow), 0 0 30px #4a6a2a;
  animation: whispPulse 2s ease-in-out infinite, whispBob 3s ease-in-out infinite;
}
@keyframes whispPulse {
  0%, 100% { box-shadow: 0 0 12px #6a8a4a, 0 0 24px #4a6a2a; transform: scale(1); }
  50% { box-shadow: 0 0 24px #8ab060, 0 0 40px #5a8a3a; transform: scale(1.05); }
}
@keyframes whispBob { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-5px); } }

/* ── RIGHT PANEL AVATAR ── */
.player-card-large { text-align: center; }
.player-avatar-large {
  max-width: 220px; width: 100%; aspect-ratio: 1/1;
  border-radius: 20px; border: 2px solid var(--gold-light);
  box-shadow: 0 0 25px #2a1a1a, inset 0 1px 3px rgba(255,255,255,0.1);
  margin: 0 auto 6px; object-fit: cover;
}

/* ── COLLAPSIBLE STATS ── */
.collapsible-stats {
  background: linear-gradient(135deg, rgba(18,10,14,0.4), rgba(12,6,10,0.2));
  border-radius: 24px; border: 2px solid var(--gold-base); margin-bottom: 10px;
}
.collapsible-header {
  background: linear-gradient(145deg, #2a1a10, #1a0a08); padding: 10px 16px;
  cursor: pointer; display: flex; justify-content: space-between; align-items: center;
  font-weight: 600; font-size: 0.8rem; border-radius: 22px 22px 0 0;
  border-bottom: 1px solid var(--gold-base); color: #e0d0b0;
}
.collapsible-content { padding: 14px; overflow: hidden; }

.stat-row {
  background: rgba(15,8,12,0.5); border-radius: 24px; border: 1px solid var(--gold-base);
  padding: 6px 12px; margin: 4px 0; display: flex; justify-content: space-between;
  font-size: 0.7rem; color: #e0d0b0;
}

/* ── RITUAL CONSOLE ── */
.ritual-console {
  background: linear-gradient(135deg, rgba(15,8,14,0.4), rgba(8,4,10,0.2));
  backdrop-filter: blur(12px); border: 2px solid var(--gold-base); border-radius: 24px;
  padding: 16px; margin-top: 10px;
}
.console-rune-slot {
  width: 48px; height: 48px; background: rgba(8,4,10,0.5); border: 1px solid var(--gold-base);
  border-radius: 14px; display: flex; align-items: center; justify-content: center; cursor: pointer;
}
.console-rune-slot.filled { border-color: #e0d0a0; box-shadow: 0 0 12px rgba(220,200,150,0.4); }
.console-rune-grid { display: flex; flex-wrap: wrap; gap: 4px; }
.console-rune-badge {
  background: #1c120c; border: 1px solid var(--gold-base); border-radius: 14px;
  padding: 4px 8px; font-size: 0.65rem; cursor: pointer; color: #f0e0c8;
}
.console-rune-badge.selected { background: #2a1a10; border-color: #e0d0a0; }

/* ── RITUAL CIRCLE ── */
.ritual-circle {
  background: radial-gradient(circle at 30% 30%, #2a1a1a, #08040c);
  border-radius: 50%; padding: 16px; position: relative;
  width: 440px; height: 440px;
  box-shadow: 0 0 60px rgba(0,0,0,0.9), inset 0 2px 8px rgba(255,255,255,0.1),
    inset 0 -3px 12px rgba(0,0,0,0.7), 0 0 0 3px var(--gold-base),
    0 0 0 5px #1a0a0a, 0 15px 35px rgba(0,0,0,0.8), 0 0 40px rgba(200,170,120,0.25);
  margin: 0 auto 16px;
}
canvas#circleCanvas {
  display: block; width: 100%; height: 100%; background: transparent; border-radius: 50%; cursor: crosshair;
}

.rune-slot {
  position: absolute; width: 56px; height: 56px;
  background: radial-gradient(circle at 35% 35%, rgba(60,30,20,0.8), rgba(8,2,5,0.9));
  border: 2px solid var(--gold-base); border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  top: 50%; left: 50%; margin: -28px 0 0 -28px; pointer-events: auto;
  box-shadow: inset 0 1px 3px rgba(255,255,255,0.1), inset 0 -1px 3px rgba(0,0,0,0.6), 0 4px 8px rgba(0,0,0,0.5);
}
.rune-slot.filled {
  border-color: #d4af37;
  box-shadow: 0 0 25px rgba(212,175,55,0.7), inset 0 0 12px rgba(212,175,55,0.3),
    inset 0 1px 3px rgba(255,255,255,0.15), 0 6px 12px rgba(0,0,0,0.6);
  animation: runeGlow 2s ease-in-out infinite;
}
@keyframes runeGlow {
  0%, 100% { box-shadow: 0 0 25px rgba(212,175,55,0.7), inset 0 0 12px rgba(212,175,55,0.3), inset 0 1px 3px rgba(255,255,255,0.15), 0 6px 12px rgba(0,0,0,0.6), 0 0 40px rgba(212,175,55,0.2); }
  50% { box-shadow: 0 0 35px rgba(212,175,55,0.9), inset 0 0 15px rgba(212,175,55,0.4), inset 0 1px 3px rgba(255,255,255,0.2), 0 6px 12px rgba(0,0,0,0.6), 0 0 60px rgba(212,175,55,0.3); }
}

/* ── CARD SLOTS ── */
.card-slot {
  width: 48px; height: 64px;
  background: linear-gradient(145deg, #2a1a0a, #0f0804);
  border: 2px solid #4a3a2a;
  border-radius: 12px; cursor: pointer; overflow: hidden; position: relative;
  box-shadow: 0 4px 0 #0a0505, inset 0 1px 2px rgba(255,255,255,0.08),
    inset 0 -1px 2px rgba(0,0,0,0.4), 0 6px 12px rgba(0,0,0,0.4);
}
.card-slot.filled { border-color: #8a7a5a; box-shadow: 0 4px 0 #0a0505, 0 6px 12px rgba(0,0,0,0.5), 0 0 12px rgba(200,170,120,0.3); }
.card-slot:hover { transform: scale(1.08) translateY(-3px); border-color: var(--gold-light); }

/* ── MODALS ── */
.modal {
  display: none; position: fixed; inset: 0;
  background: rgba(0,0,0,0.9); backdrop-filter: blur(16px); z-index: 2000;
  justify-content: center; align-items: center;
}
.modal-content {
  background: linear-gradient(135deg, #0f0805 0%, #080308 100%);
  border: 3px solid var(--gold-light);
  border-radius: 40px; padding: 28px;
  max-width: 90vw; max-height: 85vh; overflow-y: auto;
  box-shadow: 0 30px 60px rgba(0,0,0,0.9), inset 0 1px 0 rgba(255,255,255,0.1),
    inset 0 2px 8px rgba(255,255,255,0.05), inset 0 -2px 8px rgba(0,0,0,0.6),
    0 0 80px rgba(200,170,120,0.2);
  animation: modalSlideInFromBelow 0.6s var(--anim-spring) forwards;
}
@keyframes modalSlideInFromBelow {
  0% { opacity: 0; transform: translateY(200px) scale(0.92) rotateX(10deg); transform-origin: center bottom; }
  70% { transform: translateY(-10px) scale(1.02) rotateX(2deg); }
  100% { opacity: 1; transform: translateY(0) scale(1) rotateX(0); }
}

.tome-tab {
  background: linear-gradient(145deg, #2a1a10, #1a0a08); border: 2px solid var(--gold-base); color: #f0e8e0;
  padding: 8px 16px; border-radius: 28px; cursor: pointer; font-size: 0.75rem;
  box-shadow: 0 2px 0 #0a0505, inset 0 1px 2px rgba(255,255,255,0.05), 0 4px 8px rgba(0,0,0,0.3);
  transition: all 0.25s ease; font-weight: 600; letter-spacing: 0.05em;
}
.tome-tab.active { background: linear-gradient(145deg, #3a2a18, #2a1a10); border-color: #d4af37; color: #ffd700; }

/* ── RESPONSIVE ── */
@media (max-width: 1200px) {
  .main-grid { grid-template-columns: 1fr; gap: 16px; }
  .panel { transform: none !important; animation: none !important; opacity: 1 !important; }
  #gameContainer { perspective: none; }
}

/* Custom scrollbar */
::-webkit-scrollbar { width: 8px; height: 8px; }
::-webkit-scrollbar-track { background: rgba(0,0,0,0.3); border-radius: 10px; }
::-webkit-scrollbar-thumb { background: linear-gradient(180deg, #a8804a, #7a5a4a); border-radius: 10px; box-shadow: inset 0 0 6px rgba(255,255,255,0.1); }
::-webkit-scrollbar-thumb:hover { background: linear-gradient(180deg, #c8a05a, #9a7a5a); }