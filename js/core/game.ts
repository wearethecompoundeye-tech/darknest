// js/core/game.ts – Central game orchestrator, demon‑free.// Kalgoth’s taunts and modal manager are integrated.// All button bindings are present and correct.import {  state, health, will, maxWill, ingredients, crafted,  timerSeconds, initSaveSystem, loadSaveData, autoSave,  applyDailyPassives, resetDailyItemUsage, initializeState,  setAddLog, kalgothsNoose, circlePower, circleMastery,  orbexFragments, ownedCards, equippedEntitySlots,  familiar, discoveries, tutorial, isGazeActive, gazePhase,  dailyConsumableSlots, getActiveEntity} from './state-signals.js';import { initAudio, startCaveDrips, stopAllAudioProcesses, updateVolumes } from '../audio/sfx.js';import { initCircleTracing } from '../minigames/circle-trace.js';import { setupUIEffects, initOrbitAnimation, updateUI, triggerScreenPulse } from '../ui/ui-renderer.js';import { openGrimoire } from '../ui/grimoire.js';import { openSatchel } from '../ui/satchel.js';import { openTome } from '../ui/tome.js';import { initWhispChat, destroyWhispChat } from '../ui/whisp-chat.js';import { showPathSelectionModal } from '../systems/maze-system.js';import { summonEntity } from '../systems/summoning.js';import { initPlugins, destroyPlugins } from '../plugins/plugin-interface.js';import { addLog } from '../ui/log-manager.js';import { initDayCycle, stopDayCycle, payTithe, attemptEscape } from '../systems/day-cycle.js';import { initRitualMeter, destroyGemMeter } from '../ui/ritual-meter.js';import { initTutorialListeners } from '../systems/tutorial-listeners.js';import { initGemMeter } from '../ui/ritual-meter.js';import { applyWardNooseReduction } from '../systems/familiar-manager.js';import { startGazeWarning } from '../systems/gaze-event.js';import { clearAllParticles } from '../ui/particle-system.js';import { injectSummonStyles } from '../systems/summoning.js';import { ModalManager } from '../ui/modal-manager.js';import { renderSettingsContent } from '../ui/settings-panel.js';import { getKalgothAction } from '../ai/battle-ai.js';export class Game {  private started = false;  private kalgothTauntInterval: number | null = null;  constructor() {    this.start = this.start.bind(this);  }  public async start(): Promise<void> {    if (this.started) return;    this.started = true;    initializeState();    applyDailyPassives(addLog);    setAddLog(addLog);    const loaded = await initSaveSystem();    if (!loaded) autoSave();    (window as any).modalManager = new ModalManager();    initAudio();    startCaveDrips();    setupUIEffects();    initOrbitAnimation();    updateUI();    initCircleTracing();    initGemMeter();    initRitualMeter();    initTutorialListeners();    setTimeout(() => initWhispChat(), 600);    initDayCycle();    initPlugins().then(() => console.log('[Game] Plug‑ins ready'));    injectSummonStyles();    this.startKalgothTaunts();    const gameContainer = document.getElementById('gameContainer');    if (gameContainer) gameContainer.classList.add('altar-active');    this.bindUIButtons();    window.addEventListener('beforeunload', () => this.shutdown());    console.log('✅ Game ready with Altar of the Seed');  }  private startKalgothTaunts(): void {    const minInterval = 120_000;    const maxInterval = 180_000;    const schedule = () => {      const delay = minInterval + Math.random() * (maxInterval - minInterval);      this.kalgothTauntInterval = window.setTimeout(() => {        addLog(`KALGOTH: *A distant, mocking laugh echoes.*`, false, "void");        schedule();      }, delay);    };    schedule();  }