// js/core/game.ts – Central game orchestrator, demon‑free.// Kalgoth’s taunts and modal manager are integrated.// All button bindings are present and correct.import {  state, health, will, maxWill, ingredients, crafted,  timerSeconds, initSaveSystem, loadSaveData, autoSave,  applyDailyPassives, resetDailyItemUsage, initializeState,  setAddLog, kalgothsNoose, circlePower, circleMastery,  orbexFragments, ownedCards, equippedEntitySlots,  familiar, discoveries, tutorial, isGazeActive, gazePhase,  dailyConsumableSlots, getActiveEntity} from './state-signals.js';import { initAudio, startCaveDrips, stopAllAudioProcesses, updateVolumes } from '../audio/sfx.js';import { initCircleTracing } from '../minigames/circle-trace.js';import { setupUIEffects, initOrbitAnimation, updateUI, triggerScreenPulse } from '../ui/ui-renderer.js';import { openGrimoire } from '../ui/grimoire.js';import { openSatchel } from '../ui/satchel.js';import { openTome } from '../ui/tome.js';import { initWhispChat, destroyWhispChat } from '../ui/whisp-chat.js';import { showPathSelectionModal } from '../systems/maze-system.js';import { summonEntity } from '../systems/summoning.js';import { initPlugins, destroyPlugins } from '../plugins/plugin-interface.js';import { addLog } from '../ui/log-manager.js';import { initDayCycle, stopDayCycle, payTithe, attemptEscape } from '../systems/day-cycle.js';import { initRitualMeter, destroyGemMeter } from '../ui/ritual-meter.js';import { initTutorialListeners } from '../systems/tutorial-listeners.js';import { initGemMeter } from '../ui/ritual-meter.js';import { applyWardNooseReduction } from '../systems/familiar-manager.js';import { startGazeWarning } from '../systems/gaze-event.js';import { clearAllParticles } from '../ui/particle-system.js';import { injectSummonStyles } from '../systems/summoning.js';import { ModalManager } from '../ui/modal-manager.js';import { renderSettingsContent } from '../ui/settings-panel.js';export class Game {  private started = false;  private kalgothTauntInterval: number | null = null;  constructor() {    this.start = this.start.bind(this);  }  public async start(): Promise<void> {    if (this.started) return;    this.started = true;    initializeState();    applyDailyPassives(addLog);    setAddLog(addLog);    const loaded = await initSaveSystem();    if (!loaded) autoSave();    (window as any).modalManager = new ModalManager();    initAudio();    startCaveDrips();    setupUIEffects();    initOrbitAnimation();    updateUI();    initCircleTracing();    initGemMeter();    initRitualMeter();    initTutorialListeners();    setTimeout(() => initWhispChat(), 600);    initDayCycle();    initPlugins().then(() => console.log('[Game] Plugins ready'));    injectSummonStyles();    this.startKalgothTaunts();    const gameContainer = document.getElementById('gameContainer');    if (gameContainer) gameContainer.classList.add('altar-active');    this.bindUIButtons();    window.addEventListener('beforeunload', () => this.shutdown());    console.log('✅ Game ready with Altar of the Seed');  }  private startKalgothTaunts(): void {    const minInterval = 120_000;    const maxInterval = 180_000;    const schedule = () => {      const delay = minInterval + Math.random() * (maxInterval - minInterval);      this.kalgothTauntInterval = window.setTimeout(() => {        addLog(`KALGOTH: *A distant, mocking laugh echoes.*`, false, 'void');        schedule();      }, delay);    };    schedule();  }  private bindUIButtons(): void {    document.getElementById('satchelIcon')?.addEventListener('click', openSatchel);    document.getElementById('tomeIcon')?.addEventListener('click', openTome);    document.getElementById('captureJar')?.addEventListener('click', openGrimoire);    document.getElementById('settingsIcon')?.addEventListener('click', () => {      const settingsModal = document.getElementById('settingsModal');      if (settingsModal) {        renderSettingsContent();        (window as any).modalManager.open(settingsModal);      }    });    document.getElementById('mazeEntranceBtn')?.addEventListener('click', () => showPathSelectionModal());    document.getElementById('craftPowderBtn')?.addEventListener('click', () => import('../systems/crafting.js').then(m => m.craftPowder()));    document.getElementById('craftPotionBtn')?.addEventListener('click', () => import('../systems/crafting.js').then(m => m.craftPotion(true)));    document.getElementById('craftRestorativeBtn')?.addEventListener('click', () => import('../systems/crafting.js').then(m => m.craftRestorative()));    document.getElementById('studyRuneBtn')?.addEventListener('click', () => import('../systems/crafting.js').then(m => m.studyRune()));    document.getElementById('traceRuneBtn')?.addEventListener('click', () => import('../minigames/rune-etch.js').then(m => m.startRuneTracing()));    document.getElementById('quickCraftPowderBtn')?.addEventListener('click', () => import('../systems/crafting.js').then(m => m.quickCraftPowder()));    document.getElementById('quickCraftPhialBtn')?.addEventListener('click', () => import('../systems/crafting.js').then(m => m.quickCraftPhial()));    document.getElementById('titheBtn')?.addEventListener('click', payTithe);    document.getElementById('escapeBtn')?.addEventListener('click', attemptEscape);    document.getElementById('summonBtn')?.addEventListener('click', summonEntity);    for (let i = 0; i < 3; i++) {      const slot = document.getElementById(`bandolierSlot${i}`);      if (slot) slot.addEventListener('click', () => { /* gaze-ui handles this */ });    }    document.getElementById('whispSpriteClick')?.addEventListener('click', () => import('../ui/tutorial.js').then(m => m.openWhispStats()));    document.getElementById('petFamiliarBtn')?.addEventListener('click', () => import('../systems/familiar-manager.js').then(m => m.petFamiliar()));    document.getElementById('forageBtn')?.addEventListener('click', () => import('../systems/familiar-manager.js').then(m => m.forage()));    // NEW BUTTON BINDINGS    document.getElementById('traceCircleBtn')?.addEventListener('click', () => import('../minigames/circle-trace.js').then(m => m.initCircleTracing()));    document.getElementById('tutorialBtn')?.addEventListener('click', () => import('../systems/tutorial-listeners.js').then(m => m.showTutorial()));  }  private shutdown(): void {    if (this.kalgothTauntInterval) {      clearTimeout(this.kalgothTauntInterval);      this.kalgothTauntInterval = null;    }    stopAllAudioProcesses();    clearAllParticles();    destroyWhispChat();    destroyGemMeter();    destroyPlugins();    stopDayCycle();    autoSave();  }}